# yt-match — server-side secrets (NEVER commit real values; NEVER put these in the shipped client)
# Get a FREE key: Google Cloud Console → APIs & Services → enable "YouTube Data API v3" → Credentials → API key.
YT_API_KEY=

# Supabase (service role can call sf_gd_video_set)
SUPABASE_URL=https://vmbqfzxhrqxpwgidogfm.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# a shared secret the caller must send in the x-match-secret header (so only you can trigger matching)
MATCH_SECRET=
